// Variable para almacenar el ID de sesión del usuario actual
let sessionUserId = null

// Crear un "root" para renderizar la aplicación en el elemento con el ID 'root' del documento HTML
const root = ReactDOM.createRoot(document.getElementById('root'))

// Renderizar la aplicación dentro del elemento con el ID 'root'
root.render(<App />)
