// Importar la función "cors" desde el archivo llamado 'cors'.
const cors = require('./cors')

// Exportar un objeto que contiene la función "cors" bajo la clave "cors".
module.exports = {
    cors
}
