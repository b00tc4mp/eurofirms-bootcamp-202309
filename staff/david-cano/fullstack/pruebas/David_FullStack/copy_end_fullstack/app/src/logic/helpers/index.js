// Importamos una función llamada 'validate' desde un archivo llamado 'validate'.
import validate from './validate';

// Exportamos la función 'validate' para que otras partes de nuestro programa también puedan usarla.
export {
    validate
};
