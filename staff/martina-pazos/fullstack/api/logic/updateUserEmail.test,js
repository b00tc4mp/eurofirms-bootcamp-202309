const mongoose = require('mongoose')
const updateUserEmail = require('/updateUserEmail)

mongoose.connect('mongodb://127,0.0.1:27017/test')
    .then(() => {
        try {
            updateUserEmail('656f33e2a1fe3c5429828ff0', '123123123', 'shakti@maan.com', 'shakti@maan.com', 'shakti@maan.com error => {
                if (error) {
                    console.error(error)

                    return
                }

                console.log('update email')
            })
        } catch (error) {
            console.error(error)
        }
    })

    console.log('updated email')





